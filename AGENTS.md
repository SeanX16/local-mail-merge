# 项目协作约束

## 开发前必读

任何规划或实现前，必须同时阅读：

- `docs/requirements/info.md`
- `docs/requirements/HR_AI人才信息工具_明确需求整理.md`
- `docs/requirements/邮件外联工具_明确需求整理.md`
- `docs/architecture/邮件外联工具_技术方案.md`
- `docs/decisions/ADR-001-邮件草稿交付方式.md`
- `docs/handoffs/邮件工具开发交接.md`

需求冲突时，以邮件工具明确需求和 ADR 为当前实现口径，同时保留并指出上游差异。

## 产品边界

- 外部电脑可以使用 Agent 生成个性化邮件内容；公司电脑上的本工具不能调用 AI。
- 工具只创建 Outlook 草稿，不得自动发送。
- 批量创建必须只使用 `Save()`，不得逐封调用 `Display()`，不得存在 `Send()`、`sendMail`
  或其他自动发送路径。
- 用户必须能够选择发件账户、预览内容并确认后再创建草稿。
- `compatibility_test/` 是环境验证材料，不是正式批量实现入口。

## 数据与安全

- 不提交 API Key、`.env`、真实候选人邮箱、真实邮件正文、个人简历、缓存或生成结果。
- 开发和自动化测试默认使用虚构姓名与 `.test`、`.example` 地址。
- 不在日志中记录凭据或完整真实邮件正文。
- 上游数据缺乏依据时保留 `Unknown`，不根据姓名推断国籍、语言能力等敏感属性。

## 文件与提交

- 保持独立、可迁移的目录结构；不要把测试产物、报告和临时文件堆在根目录。
- README 和主要使用文档默认使用中文。
- 未得到用户明确的“提交”指令前，不执行 Git commit。
- Git commit message 使用中文，并准确描述本阶段内容。
